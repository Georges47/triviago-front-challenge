import React from 'react';

const AppBar = () => {
    return (
        <header style={{ textAlign: 'center', padding: '10px', backgroundColor: '#008080', color: 'white' }}>
            <h1>Message App</h1>
        </header>
    );
};

export default AppBar;