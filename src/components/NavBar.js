import "./NavBarStyles.css"

function NavBar() {
    return (
       <nav>
        <div>
            <ul id="navbar">
                <li> Message App </li>
            </ul>
        </div>
       </nav>
    );
};

export default NavBar;
